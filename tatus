[1mdiff --cc server.js[m
[1mindex 3b54147b,d78f144c..00000000[m
[1m--- a/server.js[m
[1m+++ b/server.js[m
[36m@@@ -19,12 -20,12 +19,21 @@@[m [mconst http       = require("http")[m
  const https      = require("https");[m
  const cloudinary = require("cloudinary").v2;[m
  const { CloudinaryStorage } = require("multer-storage-cloudinary");[m
[32m++<<<<<<< HEAD[m
[32m +const Razorpay   = require("razorpay"); // npm i razorpay[m
[32m +const { initSocket, emitStockUpdate } = require("./socket"); // npm i socket.io[m
[32m +[m
[32m +const app        = express();[m
[32m +const httpServer = require("http").createServer(app);[m
[32m +initSocket(httpServer);[m
[32m++=======[m
[32m+ const Razorpay   = require("razorpay"); // ✅ ADDED — npm i razorpay[m
[32m+ const { initSocket, emitStockUpdate } = require("./socket"); // ✅ ADDED — npm i socket.io[m
[32m+ [m
[32m+ const app        = express();[m
[32m+ const httpServer = require("http").createServer(app);[m
[32m+ initSocket(httpServer); // ✅ ADDED — sets up Socket.IO on the same HTTP server[m
[32m++>>>>>>> a02230eb (Backend updates)[m
  [m
  // ── CORS ──────────────────────────────────────────────[m
  app.use(cors({[m
[36m@@@ -66,50 -67,68 +75,114 @@@[m [mconst cloudStorage = new CloudinaryStor[m
  });[m
  const upload = multer({ storage: cloudStorage });[m
  [m
[32m++<<<<<<< HEAD[m
[32m +// ══════════════════════════════════════════════════════[m
[32m +//  MONGODB — single connection, single reconnect path[m
[32m +//  (no duplicate native MongoClient, no overlapping retry[m
[32m +//  loops — this is the version that fixed the auth-flapping)[m
[32m +// ══════════════════════════════════════════════════════[m
[32m +const MONGO_OPTS = {[m
[32m +  serverSelectionTimeoutMS: 30000,[m
[32m +  socketTimeoutMS:          45000,[m
[32m +  connectTimeoutMS:         30000,[m
[32m +  heartbeatFrequencyMS:     10000,[m
[32m +  maxPoolSize:              10,[m
[32m +  minPoolSize:              1,[m
[32m +  family:                   4,[m
[32m +  retryWrites:              true,[m
[32m +  retryReads:               true,[m
[32m +};[m
[32m +[m
[32m +let mongoConnecting = false;[m
[32m +[m
[32m +async function connectMongoose() {[m
[32m +  if (mongoConnecting || mongoose.connection.readyState === 1) return;[m
[32m +  mongoConnecting = true;[m
[32m +  try {[m
[32m +    await mongoose.connect(process.env.MONGODB_URI, MONGO_OPTS);[m
[32m +    console.log("✅ Mongoose connected | DB:", mongoose.connection.name);[m
[32m +  } catch (err) {[m
[32m +    console.error("❌ Mongoose connect failed:", err.message);[m
[32m +  } finally {[m
[32m +    mongoConnecting = false;[m
[32m++=======[m
[32m+ // ── MongoDB ───────────────────────────────────────────[m
[32m+ const MONGO_OPTS = {[m
[32m+   serverSelectionTimeoutMS: 60000,[m
[32m+   socketTimeoutMS:          60000,[m
[32m+   connectTimeoutMS:         60000,[m
[32m+   heartbeatFrequencyMS:     30000, // ← increase from 10000 to 30000[m
[32m+   maxPoolSize:              5,     // ← reduce from 10 to 5[m
[32m+   minPoolSize:              1,     // ← reduce from 2 to 1[m
[32m+   family:                   4,[m
[32m+   retryWrites:              true,[m
[32m+   retryReads:               true,[m
[32m+   // ✅ Add these for unstable networks:[m
[32m+   waitQueueTimeoutMS:       30000,[m
[32m+   maxIdleTimeMS:            270000,[m
[32m+ };[m
[32m+ // ── Mongoose connect with auto-retry ─────────────────[m
[32m+ async function connectMongoose() {[m
[32m+   try {[m
[32m+     await mongoose.connect(process.env.MONGODB_URI, MONGO_OPTS);[m
[32m+     console.log("✅ Mongoose connected");[m
[32m+   } catch (err) {[m
[32m+     console.error("❌ Mongoose connect failed:", err.message);[m
[32m+     console.log("🔄 Retrying in 10s...");[m
[32m+     setTimeout(connectMongoose, 10000);[m
    }[m
  }[m
  connectMongoose();[m
  [m
[32m+ // ── Auto-reconnect on disconnect ─────────────────────[m
[32m+ mongoose.connection.on("disconnected", () => {[m
[32m+   console.log("⚠️  Mongoose disconnected — reconnecting in 5s...");[m
[32m+   setTimeout(connectMongoose, 5000);[m
[32m+ });[m
[32m+ mongoose.connection.on("error", (err) => {[m
[32m+   console.error("❌ Mongoose error:", err.message);[m
[32m+ });[m
[32m+ mongoose.connection.on("reconnected", () => {[m
[32m+   console.log("✅ Mongoose reconnected");[m
[32m+ });[m
[32m+ [m
[32m+ // ── Native MongoClient with auto-retry ───────────────[m
[32m+ const mongoClient = new MongoClient(process.env.MONGODB_URI, {[m
[32m+   ...MONGO_OPTS,[m
[32m+   monitorCommands: false,[m
[32m+ });[m
[32m+ let db;[m
[32m+ async function connectDB() {[m
[32m+   try {[m
[32m+     await mongoClient.connect();[m
[32m+     // Use silks_db or whatever DB is in the URI[m
[32m+     const dbName = (process.env.MONGODB_URI || "").split("/").pop()?.split("?")[0] || "silks_db";[m
[32m+     db = mongoClient.db(dbName);[m
[32m+     console.log("✅ MongoDB native connected | DB:", dbName);[m
[32m+   } catch (err) {[m
[32m+     console.error("❌ MongoDB native failed:", err.message);[m
[32m+     console.log("🔄 Retrying native connection in 10s...");[m
[32m+     setTimeout(connectDB, 10000);[m
[32m++>>>>>>> a02230eb (Backend updates)[m
[32m+   }[m
[32m+ }[m
[31m -connectDB();[m
[32m++connectMongoose();[m
[32m+ [m
[32m++<<<<<<< HEAD[m
[32m +mongoose.connection.on("disconnected", () => {[m
[32m +  console.log("⚠️  Mongoose disconnected — will retry in 8s");[m
[32m +  setTimeout(connectMongoose, 8000);[m
[32m +});[m
[32m +mongoose.connection.on("error", (err) => {[m
[32m +  console.error("❌ Mongoose error:", err.message);[m
[32m +});[m
[32m +mongoose.connection.on("reconnected", () => {[m
[32m +  console.log("✅ Mongoose reconnected");[m
[32m +});[m
[32m +[m
[32m++=======[m
[32m+ // ── DB readiness check helper ─────────────────────────[m
[32m++>>>>>>> a02230eb (Backend updates)[m
  const waitForDB = () => new Promise((resolve, reject) => {[m
    if (mongoose.connection.readyState === 1) return resolve();[m
    let tries = 0;[m
[36m@@@ -123,8 -142,8 +196,13 @@@[m
    }, 500);[m
  });[m
  [m
[32m++<<<<<<< HEAD[m
[32m +const usersCol     = () => mongoose.connection.readyState === 1 ? mongoose.connection.db.collection("users") : null;[m
[32m +const addressesCol = () => mongoose.connection.readyState === 1 ? mongoose.connection.db.collection("addresses") : null;[m
[32m++=======[m
[32m+ const usersCol     = () => db?.collection("users");[m
[32m+ const addressesCol = () => db?.collection("addresses");[m
[32m++>>>>>>> a02230eb (Backend updates)[m
  [m
  // ── Models ────────────────────────────────────────────[m
  const Product = require("./models/Product");[m
[36m@@@ -163,6 -182,10 +241,13 @@@[m [mconst BACKEND_URL  = process.env.BACKEN[m
  console.log(`✅ PhonePe | ENV:${process.env.PHONEPE_ENV} | Merchant:${PHONEPE_MERCHANT_ID||"NOT SET"}`);[m
  [m
  // ── Razorpay config ───────────────────────────────────[m
[32m++<<<<<<< HEAD[m
[32m++=======[m
[32m+ // ✅ ADDED — this was missing entirely. Payment.js calls[m
[32m+ // /api/payment/create-order and /api/payment/verify, but neither[m
[32m+ // route existed anywhere in this 